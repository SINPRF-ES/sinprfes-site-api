services:
  - type: web
    name: sinprfes-site-api
    env: node
    region: oregon
    buildCommand: "cd backend && pnpm install"
    startCommand: "cd backend && pnpm start"
    autoDeploy: true
    healthCheckPath: "/health"
    envVars:
      - key: NODE_ENV
        value: production
