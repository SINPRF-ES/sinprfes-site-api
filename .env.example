# Porta local (Render ignora e injeta sua própria PORT)
PORT=3000

# URL do PostgreSQL (pode ser KingHost, Railway, Render, etc.)
DATABASE_URL=postgres://usuario:senha@host:5432/nome_do_banco

# Se o provedor exigir SSL sem checagem de CA (muito comum em PaaS)
DB_SSL=true
