/**
 * Configurações de Push para isolamento multi-app.
 */
module.exports = {
  // Identificador do App/Backend atual.
  // Deve ser 'SINDICATO' ou 'FENAPRF'.
  APP_SCOPE: 'SINDICATO',

  // Tempo de expiração de tokens (opcional, para referência)
  TOKEN_EXPIRATION_DAYS: 60
};
