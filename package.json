{
  "name": "sinprfes-monorepo",
  "private": true,
  "scripts": {
    "install:all": "pnpm install -r",
    "build:backend": "pnpm --filter backend run build",
    "build:site": "pnpm --filter site run build"
  }
}
