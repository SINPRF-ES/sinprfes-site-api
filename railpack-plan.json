{
  "build": {
    "install": "npm install -g pnpm@9.15.9 --force && pnpm install --frozen-lockfile",
    "commands": [
      "pnpm --filter $FILTER build"
    ]
  },
  "deploy": {
    "start": "pnpm --filter $FILTER start"
  }
}
